# Android release signing — TEMPLATE.
# Copy this file to `android/keystore.properties` (which is gitignored) and fill in
# the real values. NEVER commit the real keystore.properties or the .jks file.
#
# storeFile is resolved relative to the android/app module directory.
storeFile=warscythe.jks
storePassword=CHANGE_ME
keyAlias=warscythe
keyPassword=CHANGE_ME
